'use strict';

/**
 * country-export-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::country-export-page.country-export-page');
