'use strict';

/**
 * country-export-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::country-export-page.country-export-page');
